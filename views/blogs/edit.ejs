<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('../partials/head.ejs', { title }) %>
    <link rel="stylesheet" href="/css/createForm.css">
</head>

<body>
    <%- include('../partials/header.ejs')%>

    <main>
        <div class="create content">
            <div class="form-outer">
                <h3>Update Here</h3>
                <form action="/blogs/edit/<%= blog._id %>" method="post">
                    <input type="hidden" name="_method" value="PUT">
                    <div class="form-group">
                        <label for="">Title</label>
                        <input type="text" name="title" value="<%= blog.title %>">
                    </div>
                    <div class="form-group">
                        <label for="">Description</label>
                        <textarea type="text" name="description"> <%= blog.description %> </textarea>
                    </div>
                    <div class="form-group">
                        <select name="status" id="">
                            <option value="private" <%= blog.status == "private" ? "selected" : null %> >Draft</option>
                            <option value="public" <%= blog.status == "public" ? "selected" : null %>>Publish</option>
                        </select>
                    </div>
                    <button type="submit" class="create-btn">Update</button>

                </form>
            </div>
        </div>
    </main>


    <%- include('../partials/footer.ejs')%>

</body>

</html>